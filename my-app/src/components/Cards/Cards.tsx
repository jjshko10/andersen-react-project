
const Cards = (props) => {

    if (props.isSearch) {
    /*  console.log('hello');
        event.preventDefault(); */
    }

    return (
        <div>

        </div>
    )
};

export default Cards;